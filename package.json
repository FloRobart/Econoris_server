{
  "name": "econoris",
  "version": "2.0.0",
  "description": "Éconoris est une application web de gestion de finances personnelles.",
  "main": "src/server.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/server.ts",
    "test": "jest --runInBand",

    "db-start": "docker build -t econoris-db-image ./Database --build-arg postgres && docker run --name econoris-db-container -p 18200:5432 -e POSTGRES_PASSWORD=postgres -d econoris-db-image",
    "db-stop": "docker container stop econoris-db-container && docker container rm econoris-db-container && docker image rm econoris-db-image",
    "db-logs": "docker logs econoris-db-container",
    "db-connect": "psql -U postgres -h localhost econoris -p 18200"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FloRobart/Econoris.git"
  },
  "author": "FloRobart",
  "license": "GNU GPL-3.0",
  "bugs": {
    "url": "https://github.com/FloRobart/Econoris/issues"
  },
  "homepage": "https://github.com/FloRobart/Econoris#readme",
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.13.10",
    "@types/nodemailer": "^7.0.1",
    "@types/pg": "^8.11.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "argon2": "^0.44.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "nodemailer": "^7.0.7",
    "path": "^0.12.7",
    "pg": "^8.14.1",
    "strftime": "^0.10.3",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  }
}
